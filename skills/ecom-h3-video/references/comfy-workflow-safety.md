# ComfyUI 本地工作流规则

- 使用用户已经配置好的工作流，不搜索或替换模板，不覆盖原文件。
- 只允许在一次性副本中修改提示词、输入图片/视频、视频比例和时长；其他参数一律不改。
- 用 `scripts/comfy_workflow_guard.py` 检查原工作流和副本，只放行上述字段的准确 JSON Pointer；出现其他差异就不提交。
- 找不到工作流就询问名称或路径。本地提交失败只报告真实错误，不推测兼容性、不换模型、不自动转付费 API。
